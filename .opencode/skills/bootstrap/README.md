# Bootstrap Skills

This section documents skills intended for first-run repository bootstrap.

Current mapping is defined in `skills/transfer_manifest.json` under the `bootstrap` group.

Bootstrap skills are transferred in scaffold mode through transfer-contract filtering.
